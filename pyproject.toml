[project]
name = "smac-python"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.0.2",
    "pint>=0.24.4",
    "pyserial>=3.5",
]

[tool.coverage.run]
omit = [
  "tests/*",
  "**/tests/*"
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
]
